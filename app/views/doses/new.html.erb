<div class="container">
  <div class="default-card form-card">
    <h1 class="mb-3">New dose for '<%= @cocktail.name %>'</h1>
    <%= render 'form', locals: { cocktail: @cocktail, dose: @dose } %>
    <%= link_to 'Back to cocktail', cocktail_path(@cocktail), class: 'btn btn-outline-primary' %>
  </div>
</div>